minio:
  mode: standalone
  replicas: 1

  ingress:
    enabled: true
    path: /
    hosts:
      - storage.mabecenter.org
    tls:
      - hosts:
          - storage.mabecenter.org
        secretName: tls-cert
    annotations: {}
    labels: {}
    ingressClassName: "traefik"

  consoleIngress:
    enabled: true
    path: /
    hosts:
      - minio.mabecenter.org
    tls:
      - hosts:
          - minio.mabecenter.org
        secretName: tls-cert
    annotations: {}
    labels: {}
    ingressClassName: "traefik"
  
  existingSecret: minio-existing-secret
