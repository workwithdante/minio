minio:
  mode: standalone

  ingress:
    enabled: true
    path: /
    hosts:
      - minio.mabecenter.org
    tls:
      - hosts:
          - minio.mabecenter.org
        secretName: tls-cert
    annotations: {}
    labels: {}
    ingressClassName: "traefik"

  persistence:
    storageClass: "nfs"
    enabled: true
    size: 1Gi

  resources:
    requests:
      memory: 8Gi

  accessKey: "minio-access-key"
  secretKey: "minio-secret-key"