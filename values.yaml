minio:
  ingress:
    enabled: true
    ingressClassName: "traefik"  # Usar Traefik como controlador de Ingress
    annotations:
      kubernetes.io/ingress.class: "traefik"  # Especificar que usamos Traefik
    hosts:
      - host: "minio.mabecenter.org"  # El dominio donde se accederá a MinIO
        paths:
          - path: "/"
            pathType: Prefix  # El tipo de ruta (Prefix)
    tls:
      - secretName: "tls-cert"  # Certificado TLS para la conexión segura
        hosts:
          - "minio.mabecenter.org"  

  persistence:
    storageClass: "nfs"
    enabled: true
    size: 10Gi

  accessKey: "minio-access-key"
  secretKey: "minio-secret-key"