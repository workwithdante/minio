minio:
  ingress:
    enabled: true
    ingressClassName: "traefik"
    annotations:
      kubernetes.io/ingress.class: "traefik"
    hosts:
      - minio.mabecenter.org  # Simplificado a solo el nombre del host
    tls:
      - secretName: tls-cert
        hosts:
          - minio.mabecenter.org

  persistence:
    storageClass: "nfs"
    enabled: true
    size: 10Gi

  accessKey: "minio-access-key"
  secretKey: "minio-secret-key"